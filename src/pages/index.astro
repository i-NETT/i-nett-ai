---
import BaseLayout from '@layouts/BaseLayout.astro';
import ROIModal from '@components/ROIModal.astro';
import ROICalculator from '@components/ROICalculator.astro';
import FAQ from '@components/FAQ.astro';
import CTASection from '@components/CTASection.astro';
import DemoStream from '@components/DemoStream.astro';
import { SITE } from '@lib/site';

const faqs = [
  {
    q: 'What is Fortify AI?',
    a: 'Fortify AI is a managed AI offering from i-NETT. We deploy secure, compliance-ready AI infrastructure into your firm, configure it to your regulatory environment, and operate it for you. Customer data is isolated to a dedicated tenant. We do not use customer data for model training or share it across clients.'
  },
  {
    q: 'How is Fortify AI different from buying ChatGPT or Copilot licenses?',
    a: 'Consumer and prosumer AI tools were built for individuals. Fortify AI is built for firms that operate under HIPAA, GDPR, SOC, or CCPA. Each deployment is configured to your regulatory environment, isolated to your tenant, and reviewed by your compliance team. It is the difference between a personal productivity tool and a managed business system.'
  },
  {
    q: 'Who is Fortify AI built for?',
    a: 'Mid-market firms with 30 to 150 employees in healthcare, legal, financial services, and professional services. Our buyers are CEOs and owners who want AI capability without building an in-house data science team.'
  },
  {
    q: 'What does deployment look like?',
    a: 'A typical Fortify AI deployment runs in four stages: a 30 minute consultation to scope your environment, a readiness assessment, a focused two to four week pilot on the highest-leverage use cases, then a managed rollout. Your team works with our deployment lead throughout. We handle the infrastructure.'
  },
  {
    q: 'Is Fortify AI HIPAA compliant?',
    a: 'Fortify AI deployments are built for HIPAA, GDPR, SOC, and CCPA compliance. Deployments are configured to your regulatory environment and reviewed by your compliance team. And through i-NETT’s Lloyd’s of London partnership, the firms we manage can qualify for discounted cyber insurance, reflecting the strength of the managed security posture.'
  },
  {
    q: 'What geographies does i-NETT cover?',
    a: 'i-NETT is headquartered in Southern California with primary client community across San Diego, Orange County, Los Angeles, and Ventura counties. Through the Technology Assurance Group network, we extend service into 148 cities across the United States and Canada.'
  },
  {
    q: 'How quickly can we get started?',
    a: 'The first step is a 30 minute consultation. From signed engagement, most firms see their first production AI workflow live within four to six weeks. Pilot length scales with the number of workflows in scope.'
  }
];

// Person schema for the four leaders. Rendered as a supplemental JSON-LD
// block alongside the BaseLayout schema graph.
const leadersSchema = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Person',
      '@id': `${SITE.url}/#dale-stein`,
      name: 'Dale Stein',
      jobTitle: 'CEO & Partner',
      image: `${SITE.url}/assets/brand/dale-stein-headshot.jpg`,
      worksFor: { '@id': `${SITE.url}/#organization` },
      sameAs: ['https://www.linkedin.com/in/steindale/'],
      knowsAbout: ['Managed services', 'Telecommunications', 'Mid-market technology leadership'],
    },
    {
      '@type': 'Person',
      '@id': `${SITE.url}/#nick-dreyfus`,
      name: 'Nick Dreyfus',
      jobTitle: 'Vice President of Business Development',
      image: `${SITE.url}/assets/brand/nick-headshot.jpg`,
      worksFor: { '@id': `${SITE.url}/#organization` },
      sameAs: [
        'https://www.linkedin.com/in/nicholas-dreyfus/',
        SITE.podcastUrl,
        'https://www.tiktok.com/@nick.dreyfus.inett',
        'https://www.instagram.com/nick.dreyfus.inett/',
      ],
      knowsAbout: ['Managed AI', 'Cybersecurity', 'HIPAA compliance', 'Mid-market IT strategy'],
      award: 'East County Chamber of Commerce Ambassador of the Year 2025',
    },
    {
      '@type': 'Person',
      '@id': `${SITE.url}/#john-lehmkuhl`,
      name: 'John Lehmkuhl',
      jobTitle: 'Vice President of Operations',
      worksFor: { '@id': `${SITE.url}/#organization` },
      sameAs: ['https://www.linkedin.com/in/john-lehmkuhl/'],
      knowsAbout: ['Microsoft Azure', 'Amazon Web Services', 'Network security', 'IT operations'],
    },
    {
      '@type': 'Person',
      '@id': `${SITE.url}/#oscar-salazar`,
      name: 'Oscar Salazar',
      jobTitle: 'Telecom Services Manager',
      worksFor: { '@id': `${SITE.url}/#organization` },
      sameAs: ['https://www.linkedin.com/in/oscar-salazar-94488311/'],
      knowsAbout: ['Converged communications', 'Telecommunications engineering', 'Project deployment'],
    },
  ],
};
---
<BaseLayout
  title="Fortify AI by i-NETT | Managed AI and AI Governance for Mid-Market Firms"
  description="Managed AI for 30 to 150 employee firms in healthcare, legal, financial services, and professional services. Built for HIPAA, GDPR, SOC, and CCPA. Discounted cyber insurance through our Lloyd’s of London partnership. Serving Southern California, the United States, and Canada."
  ambient={true}
  faqs={faqs}
  service={{
    name: 'Fortify AI Managed AI Deployment',
    description: 'Managed AI deployment for mid-market firms in regulated industries. Configured for HIPAA, GDPR, SOC, and CCPA compliance.',
  }}
>
  <!-- HERO -->
  <section class="hero" aria-labelledby="hero-heading">
    <div class="hero__bg" aria-hidden="true">
      <div class="hero__grid"></div>
      <div class="hero__orb hero__orb--a"></div>
      <div class="hero__orb hero__orb--b"></div>
    </div>
    <div class="container hero__inner">
      <div class="hero__copy reveal">
        <a href="/insurance-policy" class="pill pill-accent hero__pill hero__pill--link">
          <span class="hero__pulse" aria-hidden="true"></span>
          A Lloyd&rsquo;s of London cyber insurance partner
          <span class="hero__pill-arrow" aria-hidden="true">&rarr;</span>
        </a>
        <h1 id="hero-heading" class="hero__title">
          Managed AI, <span class="hero__title-accent">built for the firms</span> regulators trust.
        </h1>
        <p class="hero__tagline">
          <span class="hero__tagline-word">Make</span>
          <span class="hero__tagline-word">every</span>
          <span class="hero__tagline-word">employee</span>
          <span class="hero__tagline-word">an</span>
          <span class="hero__tagline-word">AI</span>
          <span class="hero__tagline-word">expert.</span>
          <span class="hero__tagline-word hero__tagline-word--accent">Securely.</span>
        </p>
        <p class="hero__lead">
          Fortify AI deploys secure, compliance-ready AI infrastructure into healthcare,
          legal, financial services, and professional services firms across Southern California,
          the United States, and Canada. We operate it. You see results.
        </p>
        <div class="hero__ctas">
          <a href="/resources#scan" class="btn btn-accent btn-lg">
            Take the AI Readiness Scan
            <span aria-hidden="true">&rarr;</span>
          </a>
          <a href={SITE.bookingUrl} class="btn btn-outline on-dark btn-lg" target="_blank" rel="noopener">
            Book a 30 minute consultation
          </a>
        </div>
        <ul class="hero__trust">
          <li>HIPAA, GDPR, CCPA ready; aligned with SOC 2 controls</li>
          <li>Dedicated tenant, access to all major frontier model providers</li>
          <li>148 service cities across US and Canada</li>
        </ul>
        <div class="hero__offers" aria-label="Free resources">
          <a class="hero__offer" href="/ai-usage-policy">
            <span class="hero__offer-tag">Free template</span>
            <span class="hero__offer-title">An AI usage policy your team can deploy today.</span>
          </a>
          <a class="hero__offer" href="/master-prompt">
            <span class="hero__offer-tag">Free, with a 30 minute call</span>
            <span class="hero__offer-title">Get a custom AI master prompt for your business.</span>
          </a>
        </div>
      </div>

      <aside class="hero__panel" aria-label="Fortify AI brand mark">
        <div class="hero-logo" data-hero-logo>
          <div class="hero-logo__halo" aria-hidden="true"></div>
          <div class="hero-logo__scan" aria-hidden="true"></div>
          <img
            src="/assets/brand/fortify-ai-logo.png"
            alt="Fortify AI by i-NETT"
            class="hero-logo__img"
            width="640"
            height="640"
            loading="eager"
            decoding="sync"
            fetchpriority="high"
          />
        </div>
      </aside>
    </div>
  </section>

  <!-- LOGO ROW / SOCIAL PROOF -->
  <section class="proof-bar">
    <div class="container">
      <p class="proof-bar__title reveal reveal-fade">Trusted by mid-market firms across the regulated industries</p>
      <div class="proof-bar__items">
        <div class="proof-pill reveal reveal-up reveal-delay-1">
          <img src="/assets/brand/hipaa-compliant.png" alt="HIPAA Compliant badge" width="36" height="36" loading="lazy" />
          <span>HIPAA Compliant</span>
        </div>
        <div class="proof-pill reveal reveal-up reveal-delay-2">
          <img src="/assets/brand/iso-27001.png" alt="ISO 27001 badge" width="36" height="36" loading="lazy" />
          <span>ISO 27001 aligned</span>
        </div>
        <a href="/insurance-policy" class="proof-pill proof-pill--text proof-pill--link reveal reveal-up reveal-delay-3">
          <span class="proof-pill__lead">Lloyd&rsquo;s of London</span>
          <span>Cyber insurance partner</span>
        </a>
        <div class="proof-pill proof-pill--text reveal reveal-up reveal-delay-4">
          <span class="proof-pill__lead">TAG Network</span>
          <span>Member, <span data-countup="148" data-countup-suffix=" service cities, US &amp; Canada" class="countup">148 service cities, US &amp; Canada</span></span>
        </div>
      </div>
    </div>
  </section>

  <!-- WATCH FORTIFY AI IN ACTION (scripted demo) -->
  <DemoStream />

  <!-- WHAT WE DO -->
  <section class="section" id="what-we-do" aria-labelledby="what-we-do-heading">
    <div class="container">
      <div class="section-intro reveal">
        <span class="eyebrow">What we do</span>
        <h2 id="what-we-do-heading">Three things, done at the standard your industry actually requires.</h2>
        <p class="lead">
          Fortify AI is not an AI strategy deck. It is the infrastructure, the deployment, and
          the operating team that ships AI into your firm in weeks, not quarters.
        </p>
      </div>
      <div class="grid grid-3">
        <button type="button" class="card pillar-card reveal reveal-up reveal-delay-1" data-pillar-open="pillar-1" aria-haspopup="dialog">
          <span class="card-num">01</span>
          <h3>Compliance-first infrastructure</h3>
          <p>
            Each Fortify AI deployment is configured to your regulatory environment.
            Customer data is isolated to your dedicated tenant. We do not use customer
            data for model training or share it across clients.
          </p>
          <span class="pillar-card__more" aria-hidden="true">Read more <span class="pillar-card__arrow">&rarr;</span></span>
        </button>
        <button type="button" class="card pillar-card reveal reveal-up reveal-delay-2" data-pillar-open="pillar-2" aria-haspopup="dialog">
          <span class="card-num">02</span>
          <h3>Targeted, high-leverage workflows</h3>
          <p>
            We identify the two or three workflows where AI returns the most value to
            your firm in the first 90 days. Documentation, intake, summarization,
            knowledge retrieval, client communication. We ship those first.
          </p>
          <span class="pillar-card__more" aria-hidden="true">Read more <span class="pillar-card__arrow">&rarr;</span></span>
        </button>
        <button type="button" class="card pillar-card reveal reveal-up reveal-delay-3" data-pillar-open="pillar-3" aria-haspopup="dialog">
          <span class="card-num">03</span>
          <h3>Managed operations</h3>
          <p>
            i-NETT operates the deployment. Monitoring, updates, model evaluation,
            policy enforcement, and quarterly compliance review are included. Your
            team uses the system. We run it.
          </p>
          <span class="pillar-card__more" aria-hidden="true">Read more <span class="pillar-card__arrow">&rarr;</span></span>
        </button>
      </div>
    </div>

    <!-- Pillar detail modals -->
    <div class="pillar-modal" data-pillar-modal="pillar-1" hidden role="dialog" aria-modal="true" aria-labelledby="pillar-1-title">
      <div class="pillar-modal__backdrop" data-pillar-close></div>
      <div class="pillar-modal__panel" role="document">
        <header class="pillar-modal__head">
          <span class="card-num">01</span>
          <button class="pillar-modal__close" type="button" data-pillar-close aria-label="Close detail">
            <svg viewBox="0 0 24 24" width="22" height="22" focusable="false" aria-hidden="true">
              <path d="M6 6 L18 18 M18 6 L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </button>
        </header>
        <div class="pillar-modal__body">
          <div class="pillar-modal__icon" aria-hidden="true">
            <svg viewBox="0 0 64 64" width="56" height="56" focusable="false">
              <path d="M32 6 L52 14 V30 C52 44 32 58 32 58 C32 58 12 44 12 30 V14 Z" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linejoin="round"/>
              <path d="M22 32 L28 38 L42 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h2 id="pillar-1-title">Compliance-first infrastructure</h2>
          <p>
            Every Fortify AI deployment starts with your regulatory environment, not
            ours. We configure infrastructure to meet the specific compliance posture
            of your industry: HIPAA for healthcare, SOC 2 and GLBA for financial
            services, attorney-client privilege protections for legal firms, and
            CCPA, GDPR, and PIPEDA for cross-border operations.
          </p>
          <p>
            Customer data is isolated to your dedicated tenant. We do not use your
            data for model training. We do not share data across clients. Your
            compliance team reviews our deployment posture before go-live, with
            documentation maintained for regulator audits.
          </p>
          <h3 class="pillar-modal__outcomes-head">Business outcomes</h3>
          <ul class="pillar-modal__outcomes">
            <li>Compliance review completed before deployment, not after an incident</li>
            <li>Audit-ready documentation maintained continuously</li>
            <li>Dedicated tenant architecture, no shared training data</li>
            <li>Configured to your regulatory environment, not a generic compliance baseline</li>
          </ul>
          <div class="pillar-modal__cta">
            <a href={SITE.bookingUrl} class="btn btn-accent btn-lg" target="_blank" rel="noopener">
              Book a 30 minute consultation
            </a>
          </div>
        </div>
      </div>
    </div>

    <div class="pillar-modal" data-pillar-modal="pillar-2" hidden role="dialog" aria-modal="true" aria-labelledby="pillar-2-title">
      <div class="pillar-modal__backdrop" data-pillar-close></div>
      <div class="pillar-modal__panel" role="document">
        <header class="pillar-modal__head">
          <span class="card-num">02</span>
          <button class="pillar-modal__close" type="button" data-pillar-close aria-label="Close detail">
            <svg viewBox="0 0 24 24" width="22" height="22" focusable="false" aria-hidden="true">
              <path d="M6 6 L18 18 M18 6 L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </button>
        </header>
        <div class="pillar-modal__body">
          <div class="pillar-modal__icon" aria-hidden="true">
            <svg viewBox="0 0 64 64" width="56" height="56" focusable="false">
              <circle cx="32" cy="32" r="22" fill="none" stroke="currentColor" stroke-width="2.5"/>
              <path d="M32 16 V32 L42 38" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/>
              <circle cx="32" cy="32" r="3" fill="currentColor"/>
            </svg>
          </div>
          <h2 id="pillar-2-title">Targeted, high-leverage workflows</h2>
          <p>
            AI deployment fails when firms try to implement everything at once. We
            identify the two or three workflows where AI returns the most value to
            your firm in the first 90 days: documentation, intake, summarization,
            knowledge retrieval, client communication.
          </p>
          <p>
            We ship those workflows first, measure the outcomes, then expand based on
            what is actually working. No multi-year roadmap. No platform play. Just
            measurable wins in the first quarter, with the next quarter informed by
            what we learned in this one.
          </p>
          <h3 class="pillar-modal__outcomes-head">Business outcomes</h3>
          <ul class="pillar-modal__outcomes">
            <li>First production workflow live in under 30 days</li>
            <li>Two to three high-value workflows operational within 90 days</li>
            <li>Measurable hours saved per employee, documented per workflow</li>
            <li>Expansion roadmap driven by measured outcomes, not promised features</li>
          </ul>
          <div class="pillar-modal__cta">
            <a href={SITE.bookingUrl} class="btn btn-accent btn-lg" target="_blank" rel="noopener">
              Book a 30 minute consultation
            </a>
          </div>
        </div>
      </div>
    </div>

    <div class="pillar-modal" data-pillar-modal="pillar-3" hidden role="dialog" aria-modal="true" aria-labelledby="pillar-3-title">
      <div class="pillar-modal__backdrop" data-pillar-close></div>
      <div class="pillar-modal__panel" role="document">
        <header class="pillar-modal__head">
          <span class="card-num">03</span>
          <button class="pillar-modal__close" type="button" data-pillar-close aria-label="Close detail">
            <svg viewBox="0 0 24 24" width="22" height="22" focusable="false" aria-hidden="true">
              <path d="M6 6 L18 18 M18 6 L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </button>
        </header>
        <div class="pillar-modal__body">
          <div class="pillar-modal__icon" aria-hidden="true">
            <svg viewBox="0 0 64 64" width="56" height="56" focusable="false">
              <rect x="10" y="14" width="44" height="36" rx="4" fill="none" stroke="currentColor" stroke-width="2.5"/>
              <path d="M10 24 H54" stroke="currentColor" stroke-width="2.5"/>
              <circle cx="17" cy="19" r="1.4" fill="currentColor"/>
              <circle cx="22" cy="19" r="1.4" fill="currentColor"/>
              <path d="M20 36 L28 44 L44 28" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h2 id="pillar-3-title">Managed operations</h2>
          <p>
            We do not hand your team a platform and walk away. Our team operates the
            deployment: monitoring, model evaluation, prompt library maintenance,
            policy enforcement, version updates, and quarterly compliance review.
            Your team uses the system. We run it.
          </p>
          <p>
            This is the difference between buying AI software and buying AI
            operations. The former leaves you responsible for an entire new
            technology stack. The latter lets your team focus on the work that
            drives the business.
          </p>
          <h3 class="pillar-modal__outcomes-head">Business outcomes</h3>
          <ul class="pillar-modal__outcomes">
            <li>24/7 monitoring and incident response</li>
            <li>Quarterly compliance review included</li>
            <li>Model and prompt library updates handled by our team</li>
            <li>Your team uses the system, we operate the infrastructure</li>
          </ul>
          <div class="pillar-modal__cta">
            <a href={SITE.bookingUrl} class="btn btn-accent btn-lg" target="_blank" rel="noopener">
              Book a 30 minute consultation
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- THREE THINGS YOU CANNOT BUY ANYWHERE ELSE -->
  {/*
    Specific efficacy figures pending HermanScience source documentation
    and John Lehmkuhl review. Until then, copy uses directional language
    only ("dramatically reduces", "measurably improves", "significantly
    reduces"). Do not insert percentages here without sourced backing.
  */}
  <section class="section section-three" id="three-things" aria-labelledby="three-things-heading">
    <div class="container">
      <div class="section-intro reveal reveal-up">
        <span class="eyebrow">Three things you cannot buy anywhere else</span>
        <h2 id="three-things-heading">What separates Fortify AI from every other AI platform on the market.</h2>
      </div>
      <div class="three-grid">
        <button type="button" class="three-card reveal reveal-up reveal-delay-1" data-pillar-open="tt-1" aria-haspopup="dialog">
          <div class="three-card__icon" aria-hidden="true">
            <svg viewBox="0 0 32 32" width="28" height="28" focusable="false">
              <path d="M16 3 L26 7 V15 C26 22 16 28 16 28 C16 28 6 22 6 15 V7 Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/>
              <path d="M11 16 L14 19 L21 12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
          <h3>Completely secure AI</h3>
          <p>
            No other LLM platform offers this isolation posture. Your data stays in
            your tenant, with no shared training, no cross-client exposure, and
            architecture configured to your regulatory environment.
          </p>
          <span class="three-card__more" aria-hidden="true">Learn more <span class="three-card__more-icon">+</span></span>
        </button>

        <button type="button" class="three-card three-card--featured reveal reveal-up reveal-delay-2" data-pillar-open="tt-2" aria-haspopup="dialog">
          <div class="three-card__icon" aria-hidden="true">
            <svg viewBox="0 0 32 32" width="28" height="28" focusable="false">
              <circle cx="16" cy="16" r="11" fill="none" stroke="currentColor" stroke-width="2"/>
              <path d="M16 9 V16 L21 19" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
              <circle cx="16" cy="16" r="2" fill="currentColor"/>
            </svg>
          </div>
          <h3>Automations built for your team</h3>
          <p>
            Not self-serve tooling. Done-for-you operations that map to how your team
            actually works, deployed and maintained by people who understand your
            business.
          </p>
          <span class="three-card__more" aria-hidden="true">Learn more <span class="three-card__more-icon">+</span></span>
        </button>

        <button type="button" class="three-card reveal reveal-up reveal-delay-3" data-pillar-open="tt-3" aria-haspopup="dialog">
          <div class="three-card__icon" aria-hidden="true">
            <svg viewBox="0 0 32 32" width="28" height="28" focusable="false">
              <circle cx="16" cy="11" r="5" fill="none" stroke="currentColor" stroke-width="2"/>
              <path d="M7 27 C7 22 11 19 16 19 C21 19 25 22 25 27" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
              <path d="M22 8 L24 6 L27 6 L27 9 L25 11" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round"/>
            </svg>
          </div>
          <h3>A full-time prompt coach</h3>
          <p>
            Behavioral science guidance built into every interaction. Dramatically
            reduces bad answers and hallucinations. Gives employees back hours every
            week. Powered by HermanScience behavioral science methodology.
          </p>
          <span class="three-card__more" aria-hidden="true">Learn more <span class="three-card__more-icon">+</span></span>
        </button>
      </div>

      <!-- Three things detail popups (reuse the pillar-modal system) -->
      <div class="pillar-modal" data-pillar-modal="tt-1" hidden role="dialog" aria-modal="true" aria-labelledby="tt-1-title">
        <div class="pillar-modal__backdrop" data-pillar-close></div>
        <div class="pillar-modal__panel" role="document">
          <header class="pillar-modal__head">
            <span class="card-num">01</span>
            <button class="pillar-modal__close" type="button" data-pillar-close aria-label="Close detail">
              <svg viewBox="0 0 24 24" width="22" height="22" focusable="false" aria-hidden="true"><path d="M6 6 L18 18 M18 6 L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
            </button>
          </header>
          <div class="pillar-modal__body">
            <div class="pillar-modal__icon" aria-hidden="true">
              <svg viewBox="0 0 64 64" width="56" height="56" focusable="false"><path d="M32 6 L52 14 V30 C52 44 32 58 32 58 C32 58 12 44 12 30 V14 Z" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linejoin="round"/><path d="M22 32 L28 38 L42 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </div>
            <h2 id="tt-1-title">Completely secure AI</h2>
            <p>
              No other LLM platform offers this isolation posture. Every Fortify AI
              deployment runs in a dedicated tenant, so your data is never pooled with
              other clients and is never used to train shared models.
            </p>
            <p>
              The architecture is configured to your regulatory environment from the
              start, and your compliance team reviews the posture before go-live. You
              get isolation you can defend in an audit, not a checkbox you have to chase.
            </p>
            <h3 class="pillar-modal__outcomes-head">What you get</h3>
            <ul class="pillar-modal__outcomes">
              <li>Dedicated tenant, with no shared training data</li>
              <li>No cross-client exposure</li>
              <li>Architecture configured to your regulatory environment</li>
              <li>Compliance review completed before deployment</li>
            </ul>
            <div class="pillar-modal__cta">
              <a href={SITE.bookingUrl} class="btn btn-accent btn-lg" target="_blank" rel="noopener">Book a 30 minute consultation</a>
              <a href="/fortify-ai" class="btn btn-outline btn-lg">Read more on Fortify AI</a>
            </div>
          </div>
        </div>
      </div>

      <div class="pillar-modal" data-pillar-modal="tt-2" hidden role="dialog" aria-modal="true" aria-labelledby="tt-2-title">
        <div class="pillar-modal__backdrop" data-pillar-close></div>
        <div class="pillar-modal__panel" role="document">
          <header class="pillar-modal__head">
            <span class="card-num">02</span>
            <button class="pillar-modal__close" type="button" data-pillar-close aria-label="Close detail">
              <svg viewBox="0 0 24 24" width="22" height="22" focusable="false" aria-hidden="true"><path d="M6 6 L18 18 M18 6 L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
            </button>
          </header>
          <div class="pillar-modal__body">
            <div class="pillar-modal__icon" aria-hidden="true">
              <svg viewBox="0 0 64 64" width="56" height="56" focusable="false"><circle cx="32" cy="32" r="22" fill="none" stroke="currentColor" stroke-width="2.5"/><path d="M32 16 V32 L42 38" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/><circle cx="32" cy="32" r="3" fill="currentColor"/></svg>
            </div>
            <h2 id="tt-2-title">Automations built for your team</h2>
            <p>
              This is not self-serve tooling you have to learn. We design each automation
              around how your team actually works, deploy it into your environment, and
              operate it for you.
            </p>
            <p>
              We start with the two or three workflows that return the most value first.
              Documentation, intake, summarization, and client communication are common
              early wins, then we expand based on measured results, not a roadmap of promises.
            </p>
            <h3 class="pillar-modal__outcomes-head">What you get</h3>
            <ul class="pillar-modal__outcomes">
              <li>Workflows mapped to your real processes</li>
              <li>Deployed and maintained by our team</li>
              <li>First production workflow live in weeks, not quarters</li>
              <li>Expansion driven by measured outcomes</li>
            </ul>
            <div class="pillar-modal__cta">
              <a href={SITE.bookingUrl} class="btn btn-accent btn-lg" target="_blank" rel="noopener">Book a 30 minute consultation</a>
              <a href="/how-it-works" class="btn btn-outline btn-lg">Read more on how it works</a>
            </div>
          </div>
        </div>
      </div>

      <div class="pillar-modal" data-pillar-modal="tt-3" hidden role="dialog" aria-modal="true" aria-labelledby="tt-3-title">
        <div class="pillar-modal__backdrop" data-pillar-close></div>
        <div class="pillar-modal__panel" role="document">
          <header class="pillar-modal__head">
            <span class="card-num">03</span>
            <button class="pillar-modal__close" type="button" data-pillar-close aria-label="Close detail">
              <svg viewBox="0 0 24 24" width="22" height="22" focusable="false" aria-hidden="true"><path d="M6 6 L18 18 M18 6 L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
            </button>
          </header>
          <div class="pillar-modal__body">
            <div class="pillar-modal__icon" aria-hidden="true">
              <svg viewBox="0 0 64 64" width="56" height="56" focusable="false"><circle cx="32" cy="22" r="10" fill="none" stroke="currentColor" stroke-width="2.5"/><path d="M14 54 C14 44 22 38 32 38 C42 38 50 44 50 54" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/></svg>
            </div>
            <h2 id="tt-3-title">A full-time prompt coach</h2>
            <p>
              Behavioral-science guidance is built into every interaction, so your team
              gets better answers without learning prompt engineering. It dramatically
              reduces bad answers and hallucinations and gives employees back hours every week.
            </p>
            <p>
              Powered by HermanScience behavioral-science methodology, the coach nudges
              each request toward clarity, context, and the right level of detail, quietly
              raising the quality of every output across your firm.
            </p>
            <h3 class="pillar-modal__outcomes-head">What you get</h3>
            <ul class="pillar-modal__outcomes">
              <li>Fewer hallucinations and bad answers</li>
              <li>Hours saved per employee each week</li>
              <li>No prompt-engineering training required</li>
              <li>Built into every interaction by default</li>
            </ul>
            <div class="pillar-modal__cta">
              <a href={SITE.bookingUrl} class="btn btn-accent btn-lg" target="_blank" rel="noopener">Book a 30 minute consultation</a>
              <a href="/fortify-ai" class="btn btn-outline btn-lg">Read more on Fortify AI</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- WHY US -->
  <section class="section section-dark" id="why-us" aria-labelledby="why-us-heading">
    <div class="container">
      <div class="section-intro reveal">
        <span class="eyebrow">Why Fortify AI</span>
        <h2 id="why-us-heading">Three things consumer AI cannot give you.</h2>
      </div>
      <div class="grid grid-3">
        <a href="/insurance-policy" class="card card-dark why-link reveal reveal-up reveal-delay-1">
          <h3>A posture insurers reward</h3>
          <p>
            i-NETT is a Lloyd&rsquo;s of London cyber insurance partner. Because our
            managed security is rated so highly for reducing risk, the firms we manage
            can qualify for discounted cyber coverage, turning a strong posture into a
            financial advantage.
          </p>
          <span class="why-more" aria-hidden="true">See the partnership <span>&rarr;</span></span>
        </a>
        <a href="/how-it-works" class="card card-dark why-link reveal reveal-up reveal-delay-2">
          <h3>A regulatory blueprint, not a checkbox</h3>
          <p>
            HIPAA, GDPR, SOC, and CCPA are designed into deployment from day one,
            then reviewed by your compliance team. You get a configuration you can
            defend, not a vendor questionnaire you have to chase.
          </p>
          <span class="why-more" aria-hidden="true">How it works <span>&rarr;</span></span>
        </a>
        <a href="/about" class="card card-dark why-link reveal reveal-up reveal-delay-3">
          <h3>A managed services team behind the model</h3>
          <p>
            AI without operations is a liability. We bring 20 plus years of managed
            services discipline into every Fortify AI rollout, including 24/7 monitoring
            through the Technology Assurance Group network.
          </p>
          <span class="why-more" aria-hidden="true">Meet the team <span>&rarr;</span></span>
        </a>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section class="section how-section" id="how-it-works" aria-labelledby="how-heading">
    <div class="container">
      <div class="section-intro reveal">
        <span class="eyebrow">How it works</span>
        <h2 id="how-heading">From first conversation to live deployment in four to six weeks.</h2>
      </div>
      <ol class="how-steps">
        <li class="how-step reveal">
          <span class="how-step__num">1</span>
          <div>
            <h3>30 minute consultation</h3>
            <p>
              We scope your environment, your compliance footprint, and the two or
              three highest-leverage AI deployments for your firm.
            </p>
          </div>
        </li>
        <li class="how-step reveal">
          <span class="how-step__num">2</span>
          <div>
            <h3>Readiness assessment</h3>
            <p>
              We map your data sources, your existing systems, and the regulatory
              constraints that will shape the configuration. You receive a written
              readiness scan.
            </p>
          </div>
        </li>
        <li class="how-step reveal">
          <span class="how-step__num">3</span>
          <div>
            <h3>Pilot in two to four weeks</h3>
            <p>
              We deploy a focused pilot on the highest-leverage workflow. Your team
              uses it. We measure results. Your compliance team reviews.
            </p>
          </div>
        </li>
        <li class="how-step reveal">
          <span class="how-step__num">4</span>
          <div>
            <h3>Managed rollout</h3>
            <p>
              We expand to your full team and additional workflows. i-NETT operates
              the deployment, with quarterly compliance review and continuous model
              evaluation built in.
            </p>
          </div>
        </li>
      </ol>
      <div class="how-cta reveal">
        <a href="/how-it-works" class="btn btn-outline">Read the full methodology &rarr;</a>
      </div>
    </div>
  </section>

  <!-- ROI CALCULATOR (live, inline) -->
  <section class="section section-roi roi-showcase roi-steel" id="roi" aria-labelledby="roi-heading">
    <div class="roi-showcase__bg" aria-hidden="true"></div>
    <div class="container">
      <div class="roi-home-intro reveal reveal-up">
        <span class="pill pill-accent roi-showcase__badge">
          <span class="roi-showcase__pulse" aria-hidden="true"></span>
          Live ROI calculator
        </span>
        <h2 id="roi-heading">What is a five-hour-per-week productivity uplift worth to your firm?</h2>
        <p class="lead">
          Pick your industry. Drag the sliders. Type an exact employee count if you
          prefer. The number is yours to take to your board.
        </p>
      </div>
      <div class="roi-home-calc reveal reveal-up reveal-delay-1">
        <div class="roi-stage">
          <div class="roi-stage__inner">
            <ROICalculator variant="inline" id="home-roi" showMethodologyLink={true} />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Supplemental Person schema for the four leaders -->
  <script type="application/ld+json" set:html={JSON.stringify(leadersSchema)} />

  <!-- PROOF / PEOPLE -->
  <section class="section section-soft" aria-labelledby="people-heading">
    <div class="container">
      <div class="section-intro reveal">
        <span class="eyebrow">Built by operators</span>
        <h2 id="people-heading">The team behind Fortify AI.</h2>
      </div>
      {/*
        Team grid 2x2. John and Oscar headshots pending download from i-nett.com.
        Until then, placeholder circles with initials in i-NETT blue render.
        Replace the .leader-card__placeholder div with an <img> when assets land.
      */}
      <div class="leaders-grid">
        <article class="leader-card reveal reveal-up reveal-delay-1">
          <img src="/assets/brand/dale-stein-headshot.jpg" alt="Dale Stein, CEO and Partner at i-NETT" width="140" height="140" loading="lazy" class="leader-card__photo" />
          <h3>Dale Stein</h3>
          <p class="leader-card__role">CEO &amp; Partner, i-NETT</p>
          <p>
            Dale leads i-NETT and co-founded Technology Assurance Group (TAG), a network
            of leading Managed Technology Services Providers representing over $1 billion
            in combined annual products and services across North America. His strategic
            vision guides both organizations.
          </p>
          <p>
            Prior to i-NETT, Dale served as CEO of INET, Inc., growing it into the #1
            distributor of Mitel products in North America. He founded Westec Security
            Corporation and built it to more than $30 million in annual revenues. He
            holds a Bachelor&rsquo;s degree from DeVry and completed Harvard&rsquo;s Small
            Company Management Program. He serves on the Chairmen&rsquo;s Roundtable in
            San Diego.
          </p>
          <ul class="leader-card__social" aria-label="Dale Stein social links">
            <li>
              <a href="https://www.linkedin.com/in/steindale/" target="_blank" rel="noopener" aria-label="Dale Stein on LinkedIn">
                <svg viewBox="0 0 24 24" width="20" height="20" focusable="false" aria-hidden="true"><path fill="currentColor" d="M19 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V5a2 2 0 0 0-2-2zM8 17H5.5v-7H8v7zM6.75 8.75A1.5 1.5 0 1 1 6.75 5.75a1.5 1.5 0 0 1 0 3zM18 17h-2.5v-3.5c0-2-2.5-1.86-2.5 0V17H10.5v-7H13v1.2c1.16-2.16 5-2.32 5 2.06V17z"/></svg>
                <span>LinkedIn</span>
              </a>
            </li>
          </ul>
        </article>

        <article class="leader-card reveal reveal-up reveal-delay-2">
          <img src="/assets/brand/nick-headshot.jpg" alt="Nick Dreyfus, VP of Business Development at i-NETT" width="140" height="140" loading="lazy" class="leader-card__photo" />
          <h3>
            Nick Dreyfus
            <span class="leader-card__badge" title="East County Chamber of Commerce Ambassador of the Year 2025">Chamber Ambassador 2025</span>
          </h3>
          <p class="leader-card__role">Vice President of Business Development, i-NETT</p>
          <p>
            Nick brings over 12 years as a systems administrator plus a decade of consulting
            and network architecture experience to his role. He focuses on understanding each
            client&rsquo;s unique culture and operational needs before developing customized
            technology strategies in managed services, cloud, AI, and cybersecurity.
          </p>
          <p>
            Nick hosts The Digital Dilemma podcast, where he interviews founders, technologists,
            and CEOs on the operational realities of running a modern business. He was named
            East County Chamber of Commerce Ambassador of the Year for 2025. He holds a
            Bachelor of Science in Network Engineering from Coleman University.
          </p>
          <ul class="leader-card__social" aria-label="Nick Dreyfus links">
            <li>
              <a href="https://www.linkedin.com/in/nicholas-dreyfus/" target="_blank" rel="noopener" aria-label="Nick Dreyfus on LinkedIn">
                <svg viewBox="0 0 24 24" width="20" height="20" focusable="false" aria-hidden="true"><path fill="currentColor" d="M19 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V5a2 2 0 0 0-2-2zM8 17H5.5v-7H8v7zM6.75 8.75A1.5 1.5 0 1 1 6.75 5.75a1.5 1.5 0 0 1 0 3zM18 17h-2.5v-3.5c0-2-2.5-1.86-2.5 0V17H10.5v-7H13v1.2c1.16-2.16 5-2.32 5 2.06V17z"/></svg>
                <span>LinkedIn</span>
              </a>
            </li>
            <li>
              <a href={SITE.bookingUrl} target="_blank" rel="noopener" aria-label="Book a meeting with Nick Dreyfus">
                <svg viewBox="0 0 24 24" width="20" height="20" focusable="false" aria-hidden="true"><path fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" d="M5 5 H19 V19 H5 Z M5 9 H19 M8 3 V7 M16 3 V7"/></svg>
                <span>Book a meeting</span>
              </a>
            </li>
            <li>
              <a href={SITE.podcastUrl} target="_blank" rel="noopener" aria-label="The Digital Dilemma podcast">
                <svg viewBox="0 0 24 24" width="20" height="20" focusable="false" aria-hidden="true"><path fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" d="M12 3 a4 4 0 0 1 4 4 V12 a4 4 0 0 1 -8 0 V7 a4 4 0 0 1 4 -4z M6 11 V13 A6 6 0 0 0 18 13 V11 M12 19 V22 M9 22 H15"/></svg>
                <span>Podcast</span>
              </a>
            </li>
            <li>
              <a href="https://www.tiktok.com/@nick.dreyfus.inett" target="_blank" rel="noopener" aria-label="Nick Dreyfus on TikTok">
                <svg viewBox="0 0 24 24" width="20" height="20" focusable="false" aria-hidden="true"><path fill="currentColor" d="M19.5 6.7a5.6 5.6 0 0 1-3.4-1.2 5.6 5.6 0 0 1-2-3.5h-3.4v12.3a2.7 2.7 0 1 1-2.7-2.7c.3 0 .6 0 .9.1V8.2c-.3 0-.6-.1-.9-.1A6.3 6.3 0 1 0 14 14.3V8.6a8.9 8.9 0 0 0 5.5 1.9V6.7z"/></svg>
                <span>TikTok</span>
              </a>
            </li>
            <li>
              <a href="https://www.instagram.com/nick.dreyfus.inett/" target="_blank" rel="noopener" aria-label="Nick Dreyfus on Instagram">
                <svg viewBox="0 0 24 24" width="20" height="20" focusable="false" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="5" fill="none" stroke="currentColor" stroke-width="2"/><circle cx="12" cy="12" r="4" fill="none" stroke="currentColor" stroke-width="2"/><circle cx="17.5" cy="6.5" r="1.2" fill="currentColor"/></svg>
                <span>Instagram</span>
              </a>
            </li>
          </ul>
        </article>

        <article class="leader-card reveal reveal-up reveal-delay-3">
          {/* Replace placeholder with John's headshot once Nick provides it. */}
          <div class="leader-card__placeholder" aria-hidden="true">JL</div>
          <h3>John Lehmkuhl</h3>
          <p class="leader-card__role">Vice President of Operations, i-NETT</p>
          <p>
            John brings over 20 years of IT management experience to his role overseeing
            i-NETT&rsquo;s day-to-day operations, technical engineers, and 24/7/365
            multi-state service group. His technical skill set spans Microsoft Azure,
            Amazon Web Services, network security, virtualization, and database management,
            with high-level coding in multiple languages.
          </p>
          <p>
            John mentors the technical team through certifications and ongoing training.
            He has worked with nearly all verticals and a vast array of software and
            hardware platforms. He actively participates in TAG initiatives, industry
            conferences, and technology partner ecosystems.
          </p>
          <ul class="leader-card__social" aria-label="John Lehmkuhl social links">
            <li>
              <a href="https://www.linkedin.com/in/john-lehmkuhl/" target="_blank" rel="noopener" aria-label="John Lehmkuhl on LinkedIn">
                <svg viewBox="0 0 24 24" width="20" height="20" focusable="false" aria-hidden="true"><path fill="currentColor" d="M19 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V5a2 2 0 0 0-2-2zM8 17H5.5v-7H8v7zM6.75 8.75A1.5 1.5 0 1 1 6.75 5.75a1.5 1.5 0 0 1 0 3zM18 17h-2.5v-3.5c0-2-2.5-1.86-2.5 0V17H10.5v-7H13v1.2c1.16-2.16 5-2.32 5 2.06V17z"/></svg>
                <span>LinkedIn</span>
              </a>
            </li>
          </ul>
        </article>

        <article class="leader-card reveal reveal-up reveal-delay-4">
          {/* Replace placeholder with Oscar's headshot once Nick provides it. */}
          <div class="leader-card__placeholder" aria-hidden="true">OS</div>
          <h3>Oscar Salazar</h3>
          <p class="leader-card__role">Telecom Services Manager, i-NETT</p>
          <p>
            Oscar brings over four decades of progressive experience in telecommunications
            and IT industries. Since joining i-NETT in 2003, he has held multiple concurrent
            roles, currently serving as Telecom Services Manager where he oversees a broad
            range of technical and managerial responsibilities.
          </p>
          <p>
            With expert-level knowledge in converged communication systems, Oscar manages
            both in-house and field engineering teams, spearheading project development
            from initial design through successful deployment. He has developed and
            implemented comprehensive policies and procedures that optimize operations and
            improve service delivery.
          </p>
          <ul class="leader-card__social" aria-label="Oscar Salazar social links">
            <li>
              <a href="https://www.linkedin.com/in/oscar-salazar-94488311/" target="_blank" rel="noopener" aria-label="Oscar Salazar on LinkedIn">
                <svg viewBox="0 0 24 24" width="20" height="20" focusable="false" aria-hidden="true"><path fill="currentColor" d="M19 3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V5a2 2 0 0 0-2-2zM8 17H5.5v-7H8v7zM6.75 8.75A1.5 1.5 0 1 1 6.75 5.75a1.5 1.5 0 0 1 0 3zM18 17h-2.5v-3.5c0-2-2.5-1.86-2.5 0V17H10.5v-7H13v1.2c1.16-2.16 5-2.32 5 2.06V17z"/></svg>
                <span>LinkedIn</span>
              </a>
            </li>
          </ul>
        </article>
      </div>
    </div>
  </section>

  <!-- INDUSTRIES -->
  <section class="section" aria-labelledby="industries-heading">
    <div class="container">
      <div class="section-intro reveal">
        <span class="eyebrow">Industries</span>
        <h2 id="industries-heading">Built for regulated, relationship-driven firms.</h2>
      </div>
      <div class="grid grid-4">
        <a href="/industries/healthcare" class="card reveal reveal-up reveal-delay-1 industry-card">
          <h3>Healthcare</h3>
          <p>HIPAA-aligned AI for practices and provider groups.</p>
          <span class="arrow" aria-hidden="true">&rarr;</span>
        </a>
        <a href="/industries/legal" class="card reveal reveal-up reveal-delay-2 industry-card">
          <h3>Legal</h3>
          <p>Privileged-information-safe AI for law firms.</p>
          <span class="arrow" aria-hidden="true">&rarr;</span>
        </a>
        <a href="/industries/financial-services" class="card reveal reveal-up reveal-delay-3 industry-card">
          <h3>Financial Services</h3>
          <p>Regulator-ready AI for advisors and firms.</p>
          <span class="arrow" aria-hidden="true">&rarr;</span>
        </a>
        <a href="/industries/professional-services" class="card reveal reveal-up reveal-delay-4 industry-card">
          <h3>Professional Services</h3>
          <p>Confidentiality-first AI for consulting and accounting.</p>
          <span class="arrow" aria-hidden="true">&rarr;</span>
        </a>
      </div>
    </div>
  </section>

  <!-- PODCAST -->
  {/*
    Digital Dilemma logo upgrade pending official asset from Nick.
    Until then, use the typographic treatment below alongside a
    microphone glyph.
  */}
  <section class="section section-dark dd-section" aria-labelledby="podcast-heading">
    <div class="container">
      <div class="dd-head reveal reveal-up">
        <span class="eyebrow">From the podcast</span>
        <h2 id="podcast-heading">The Digital Dilemma</h2>
        <p class="lead">
          Nick Dreyfus interviews founders, technologists, and CEOs on the operational
          realities of running a modern business. AI, cybersecurity, and what actually
          ships.
        </p>

        <div class="dd-logotype reveal reveal-fade reveal-delay-1" aria-hidden="true">
          <svg class="dd-logotype__mic" viewBox="0 0 24 24" width="22" height="22" focusable="false">
            <path fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" d="M12 3 a4 4 0 0 1 4 4 V12 a4 4 0 0 1 -8 0 V7 a4 4 0 0 1 4 -4z M6 11 V13 A6 6 0 0 0 18 13 V11 M12 19 V22 M9 22 H15"/>
          </svg>
          <span class="dd-logotype__words">The Digital Dilemma</span>
        </div>
      </div>

      <div class="dd-video-wrap reveal reveal-up reveal-delay-2">
        <div class="dd-video">
          <iframe
            title="Latest Digital Dilemma Podcast Episode"
            src="https://www.youtube.com/embed/IstQKqxCRQY?rel=0"
            loading="lazy"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
            allowfullscreen
            referrerpolicy="strict-origin-when-cross-origin"
          ></iframe>
        </div>
      </div>

      <div class="dd-ctas reveal reveal-up reveal-delay-3">
        <a href={SITE.podcastUrl} class="btn btn-accent btn-lg" target="_blank" rel="noopener">
          Listen on Apple Podcasts
        </a>
        <a href={SITE.bookingUrl} class="btn btn-outline on-dark btn-lg" target="_blank" rel="noopener">
          Book a 30 min consultation
        </a>
        <a href={SITE.parentUrl} class="btn btn-ghost on-dark" target="_blank" rel="noopener">
          Visit i-nett.com <span aria-hidden="true">&rarr;</span>
        </a>
      </div>
    </div>
  </section>

  <FAQ items={faqs} />

  <script>
    // Pause the hero gradient mesh once the hero scrolls out of view.
    // Saves frames on long pages and respects user attention.
    const hero = document.querySelector<HTMLElement>('.hero');
    if (hero && 'IntersectionObserver' in window) {
      const io = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            hero.classList.toggle('is-paused', !entry.isIntersecting);
          });
        },
        { threshold: 0 }
      );
      io.observe(hero);
    }

    // Pillar detail modals. Open on card click, close on backdrop / X / Esc,
    // trap focus inside the panel, restore focus to the opener on close.
    const pillarOpeners = document.querySelectorAll<HTMLElement>('[data-pillar-open]');
    const pillarModals = document.querySelectorAll<HTMLElement>('[data-pillar-modal]');
    let pillarLastFocus: HTMLElement | null = null;

    const openPillar = (id: string, opener: HTMLElement) => {
      const modal = document.querySelector<HTMLElement>(`[data-pillar-modal="${id}"]`);
      if (!modal) return;
      pillarLastFocus = opener;
      modal.hidden = false;
      document.documentElement.style.overflow = 'hidden';
      const closeBtn = modal.querySelector<HTMLElement>('.pillar-modal__close');
      closeBtn?.focus();
    };

    const closePillar = (modal: HTMLElement) => {
      modal.hidden = true;
      // Only restore scroll if no other modal is open.
      const anyOpen = Array.from(pillarModals).some((m) => !m.hidden);
      if (!anyOpen) document.documentElement.style.overflow = '';
      pillarLastFocus?.focus();
    };

    pillarOpeners.forEach((opener) => {
      opener.addEventListener('click', () => {
        const id = opener.getAttribute('data-pillar-open');
        if (id) openPillar(id, opener);
      });
    });

    pillarModals.forEach((modal) => {
      modal.querySelectorAll<HTMLElement>('[data-pillar-close]').forEach((c) =>
        c.addEventListener('click', () => closePillar(modal))
      );
      modal.addEventListener('keydown', (e) => {
        if (modal.hidden) return;
        const ke = e as KeyboardEvent;
        if (ke.key === 'Tab') {
          const focusables = modal.querySelectorAll<HTMLElement>(
            'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
          );
          if (!focusables.length) return;
          const first = focusables[0];
          const last = focusables[focusables.length - 1];
          if (ke.shiftKey && document.activeElement === first) {
            e.preventDefault(); last.focus();
          } else if (!ke.shiftKey && document.activeElement === last) {
            e.preventDefault(); first.focus();
          }
        }
      });
    });

    document.addEventListener('keydown', (e) => {
      if (e.key !== 'Escape') return;
      pillarModals.forEach((m) => { if (!m.hidden) closePillar(m); });
    });
  </script>

  <CTASection
    heading="Ready to evaluate Fortify AI for your firm?"
    body="Book a 30 minute consultation. We will scope your environment, identify the highest-leverage AI deployments for your team, and outline what a Fortify AI rollout looks like."
  />
</BaseLayout>

<style>
  /* HERO */
  .hero {
    position: relative;
    background: var(--color-ink-900);
    color: var(--color-text-on-dark);
    padding: clamp(2.25rem, 4vw + 0.5rem, 5rem) 0 clamp(3rem, 5vw + 1rem, 5.5rem);
    overflow: hidden;
    isolation: isolate;
  }
  .hero__bg {
    position: absolute;
    inset: 0;
    z-index: -1;
    overflow: hidden;
  }
  .hero__grid {
    position: absolute;
    inset: -1px;
    background-image:
      linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: radial-gradient(ellipse 80% 60% at 30% 40%, #000 40%, transparent 80%);
  }
  .hero__orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.45;
    will-change: transform;
  }
  .hero__orb--a {
    width: 520px; height: 520px;
    background: radial-gradient(circle, #00B4FF 0%, transparent 70%);
    top: -120px; left: -120px;
    animation: orb-drift-a 18s var(--ease-in-out) infinite;
  }
  .hero__orb--b {
    width: 600px; height: 600px;
    background: radial-gradient(circle, #11355C 0%, transparent 70%);
    bottom: -200px; right: -150px;
    opacity: 0.6;
    animation: orb-drift-b 24s var(--ease-in-out) infinite;
  }
  @keyframes orb-drift-a {
    0%, 100% { transform: translate3d(0, 0, 0) scale(1); }
    33% { transform: translate3d(60px, 40px, 0) scale(1.08); }
    66% { transform: translate3d(-30px, 60px, 0) scale(0.96); }
  }
  @keyframes orb-drift-b {
    0%, 100% { transform: translate3d(0, 0, 0) scale(1); }
    50% { transform: translate3d(-80px, -50px, 0) scale(1.1); }
  }
  .hero.is-paused .hero__orb { animation-play-state: paused; }
  @media (prefers-reduced-motion: reduce) {
    .hero__orb { animation: none; }
  }

  .hero__inner {
    display: grid;
    gap: var(--space-8);
    grid-template-columns: 1fr;
    align-items: center;
  }
  @media (min-width: 1024px) {
    .hero__inner { grid-template-columns: 1.3fr 1fr; gap: var(--space-9); }
  }

  .hero__pill {
    margin-bottom: var(--space-5);
    background: rgba(0,180,255,0.12);
    border-color: rgba(0,180,255,0.35);
    color: var(--color-text-on-dark);
  }
  .hero__pill--link {
    cursor: pointer;
    transition: background var(--dur-fast) var(--ease-out), border-color var(--dur-fast) var(--ease-out), transform var(--dur-fast) var(--ease-out);
  }
  .hero__pill--link:hover {
    background: rgba(0, 180, 255, 0.22);
    border-color: rgba(0, 224, 255, 0.6);
    color: var(--color-text-on-dark);
    transform: translateY(-1px);
  }
  .hero__pill-arrow {
    color: var(--color-glow);
    margin-left: 0.25em;
    transition: transform var(--dur-fast) var(--ease-out);
  }
  .hero__pill--link:hover .hero__pill-arrow {
    transform: translateX(3px);
  }
  .hero__pulse {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--color-glow);
    box-shadow: 0 0 0 0 rgba(0,224,255,0.7);
    animation: pulse 2.4s ease-out infinite;
    display: inline-block;
  }
  @keyframes pulse {
    0% { box-shadow: 0 0 0 0 rgba(0,224,255,0.6); }
    70% { box-shadow: 0 0 0 14px rgba(0,224,255,0); }
    100% { box-shadow: 0 0 0 0 rgba(0,224,255,0); }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero__pulse { animation: none; }
  }

  .hero__title {
    font-size: var(--fs-hero);
    line-height: 1.02;
    letter-spacing: -0.035em;
    font-weight: 800;
    color: #fff;
    margin-bottom: var(--space-4);
  }
  .hero__title-accent {
    background: linear-gradient(135deg, #00E0FF 0%, #00B4FF 60%, #66D6FF 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  .hero__tagline {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(1.125rem, 0.95rem + 0.75vw, 1.5rem);
    color: #fff;
    letter-spacing: -0.01em;
    margin-bottom: var(--space-5);
    line-height: 1.3;
  }
  .hero__tagline-word {
    display: inline-block;
    opacity: 0;
    transform: translateY(8px);
    animation: tagline-word 600ms var(--ease-out) forwards;
    margin-right: 0.28em;
  }
  .hero__tagline-word:nth-child(1) { animation-delay: 700ms; }
  .hero__tagline-word:nth-child(2) { animation-delay: 800ms; }
  .hero__tagline-word:nth-child(3) { animation-delay: 900ms; }
  .hero__tagline-word:nth-child(4) { animation-delay: 1000ms; }
  .hero__tagline-word:nth-child(5) { animation-delay: 1100ms; }
  .hero__tagline-word:nth-child(6) { animation-delay: 1200ms; }
  .hero__tagline-word:nth-child(7) { animation-delay: 1450ms; }
  .hero__tagline-word--accent {
    background: linear-gradient(135deg, #00E0FF 0%, #00B4FF 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  @keyframes tagline-word {
    to { opacity: 1; transform: translateY(0); }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero__tagline-word { opacity: 1; transform: none; animation: none; }
  }
  .hero__lead {
    font-size: var(--fs-md);
    color: var(--color-text-on-dark-soft);
    max-width: 56ch;
    margin-bottom: var(--space-6);
  }
  .hero__offers {
    margin-top: var(--space-6);
    display: grid;
    gap: var(--space-3);
    grid-template-columns: 1fr;
  }
  @media (min-width: 720px) { .hero__offers { grid-template-columns: 1fr 1fr; } }
  .hero__offer {
    display: block;
    padding: var(--space-4) var(--space-5);
    border-radius: var(--radius-md);
    border: 1px solid rgba(255,255,255,0.15);
    background: rgba(255,255,255,0.04);
    color: var(--color-text-on-dark);
    text-decoration: none;
    transition: border-color var(--dur-base) var(--ease-out), background var(--dur-base) var(--ease-out);
  }
  .hero__offer:hover { border-color: var(--color-accent-500); background: rgba(0,180,255,0.08); }
  .hero__offer-tag {
    display: block;
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-weight: 600;
    color: var(--color-accent-400);
    margin-bottom: var(--space-1);
  }
  .hero__offer-title { display: block; font-weight: 600; font-size: var(--fs-sm); }
  .hero__ctas {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    margin-bottom: var(--space-6);
  }
  @media (min-width: 480px) {
    .hero__ctas { flex-direction: row; }
  }
  .hero__trust {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: var(--space-2);
    color: var(--color-text-on-dark-soft);
    font-size: var(--fs-sm);
  }
  .hero__trust li {
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }
  .hero__trust li::before {
    content: '';
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--color-glow);
    flex-shrink: 0;
  }
  @media (min-width: 640px) {
    .hero__trust { grid-template-columns: repeat(3, 1fr); }
  }

  /* Hero logo lockup */
  .hero__panel {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 320px;
  }
  .hero-logo {
    position: relative;
    width: 100%;
    max-width: 520px;
    aspect-ratio: 1 / 1;
    display: grid;
    place-items: center;
    isolation: isolate;
  }
  .hero-logo__img {
    position: relative;
    z-index: 2;
    width: 100%;
    height: auto;
    max-width: 480px;
    object-fit: contain;
    opacity: 0;
    transform: scale(0.92);
    animation: hero-logo-in 1100ms var(--ease-out) 200ms forwards;
    filter: drop-shadow(0 30px 60px rgba(0, 180, 255, 0.18));
  }
  .hero-logo__halo {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: radial-gradient(circle at 50% 50%, rgba(0, 224, 255, 0.35) 0%, rgba(0, 180, 255, 0.16) 30%, transparent 65%);
    opacity: 0;
    animation: hero-halo-in 1600ms var(--ease-out) 400ms forwards, hero-halo-pulse 6s var(--ease-in-out) 2.4s infinite;
    filter: blur(20px);
  }
  .hero-logo__scan {
    position: absolute;
    inset: 0;
    z-index: 3;
    pointer-events: none;
    background: linear-gradient(110deg, transparent 38%, rgba(0, 224, 255, 0.42) 50%, transparent 62%);
    mix-blend-mode: screen;
    transform: translateX(-120%);
    /* Sweep takes ~1.25s, then idles, repeating every 5s to catch the eye
       of anyone lingering at the top. First sweep keeps the 1.2s entrance delay. */
    animation: hero-scan 5s linear 1.2s infinite;
    mask-image: radial-gradient(circle at 50% 50%, #000 30%, transparent 75%);
    -webkit-mask-image: radial-gradient(circle at 50% 50%, #000 30%, transparent 75%);
  }
  .hero.is-paused .hero-logo__scan { animation-play-state: paused; }
  @keyframes hero-logo-in {
    to { opacity: 1; transform: scale(1); }
  }
  @keyframes hero-halo-in {
    to { opacity: 1; }
  }
  @keyframes hero-halo-pulse {
    0%, 100% { transform: scale(1); opacity: 0.85; }
    50% { transform: scale(1.06); opacity: 1; }
  }
  @keyframes hero-scan {
    0%   { transform: translateX(-120%); opacity: 0; }
    3%   { opacity: 1; }
    25%  { transform: translateX(120%); opacity: 1; }
    28%  { transform: translateX(120%); opacity: 0; }
    100% { transform: translateX(120%); opacity: 0; }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-logo__img { opacity: 1; transform: none; animation: none; }
    .hero-logo__halo { opacity: 0.7; animation: none; }
    .hero-logo__scan { display: none; }
  }

  /* PROOF BAR */
  .proof-bar {
    padding: var(--space-7) 0;
    border-bottom: 1px solid var(--color-border);
    background: var(--color-bg);
  }
  .proof-bar__title {
    text-align: center;
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-text-muted);
    margin-bottom: var(--space-5);
    font-weight: 600;
  }
  .proof-bar__items {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-4);
    align-items: stretch;
  }
  .proof-pill {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    background: var(--color-bg-soft);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-3) var(--space-4);
    font-size: var(--fs-sm);
    color: var(--color-ink-700);
    font-weight: 500;
  }
  .proof-pill img {
    width: 36px; height: 36px;
    object-fit: contain;
  }
  .proof-pill--text { flex-direction: column; align-items: flex-start; gap: 2px; }
  .proof-pill--link {
    text-decoration: none;
    color: var(--color-ink-700);
    transition: border-color 200ms var(--ease-out), background 200ms var(--ease-out), transform 200ms var(--ease-out);
  }
  .proof-pill--link:hover {
    border-color: var(--color-accent-500);
    background: rgba(0, 180, 255, 0.06);
    transform: translateY(-2px);
    color: var(--color-ink-800);
  }
  .proof-pill--link:hover .proof-pill__lead { color: var(--color-accent-500); }
  .proof-pill__lead {
    font-family: var(--font-display);
    font-weight: 700;
    color: var(--color-ink-800);
    font-size: var(--fs-sm);
  }

  /* SECTION INTROS */
  .section-intro {
    max-width: 64ch;
    margin: 0 auto var(--space-8);
    text-align: center;
  }
  .section-intro .lead {
    margin-left: auto;
    margin-right: auto;
    margin-top: var(--space-4);
  }

  /* CARD numbers */
  .card-num {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: var(--fs-xs);
    color: var(--color-accent-500);
    font-weight: 600;
    margin-bottom: var(--space-3);
    letter-spacing: 0.1em;
  }
  .section-dark .card-num { color: var(--color-glow); }
  .card h3 { margin-bottom: var(--space-3); }

  /* HOW STEPS */
  .how-steps {
    list-style: none;
    padding: 0;
    margin: 0;
    max-width: 760px;
    margin-left: auto;
    margin-right: auto;
    counter-reset: step;
  }
  .how-step {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-5);
    padding: var(--space-5) 0;
    border-bottom: 1px solid var(--color-border);
  }
  .how-step:last-child { border-bottom: 0; }
  .how-step__num {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--color-ink-800);
    color: #fff;
    font-family: var(--font-display);
    font-weight: 700;
    display: inline-grid;
    place-items: center;
    font-size: var(--fs-md);
  }
  .how-step h3 { margin-bottom: var(--space-2); }
  .how-cta { text-align: center; margin-top: var(--space-7); }

  /* ROI section: full-width inline calculator */
  .roi-home-intro { max-width: 760px; margin: 0 auto var(--space-7); text-align: center; }
  .roi-home-intro h2 { margin-top: var(--space-2); }
  .roi-home-intro .lead { margin-top: var(--space-4); }
  .roi-home-calc { max-width: 1000px; margin: 0 auto; }

  /* ===== ROI SHOWCASE: dark glowing console + 3D floating stage ===== */
  .section-roi {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    background:
      radial-gradient(1100px 520px at 50% -8%, #0E3A5F 0%, rgba(14,58,95,0) 60%),
      linear-gradient(180deg, var(--color-ink-900) 0%, #07101D 100%);
    color: var(--color-text-on-dark);
  }
  .roi-showcase__bg { position: absolute; inset: 0; z-index: -1; pointer-events: none; }
  .roi-showcase__bg::before {
    content: ''; position: absolute; inset: -1px;
    background-image:
      linear-gradient(rgba(255,255,255,0.045) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.045) 1px, transparent 1px);
    background-size: 46px 46px;
    -webkit-mask-image: radial-gradient(ellipse 78% 68% at 50% 34%, #000 28%, transparent 76%);
    mask-image: radial-gradient(ellipse 78% 68% at 50% 34%, #000 28%, transparent 76%);
  }
  .roi-showcase__bg::after {
    content: ''; position: absolute; left: 50%; top: 40%;
    width: 760px; height: 520px; transform: translate(-50%, -50%);
    background: radial-gradient(closest-side, rgba(0,180,255,0.20), transparent 72%);
  }
  .roi-showcase .roi-home-intro h2 { color: #fff; }
  .roi-showcase .roi-home-intro .lead { color: var(--color-text-on-dark-soft); }
  .roi-showcase__badge {
    background: rgba(0,180,255,0.14);
    border-color: rgba(0,224,255,0.4);
    color: #fff;
  }
  .roi-showcase__pulse {
    width: 8px; height: 8px; border-radius: 50%;
    background: var(--color-glow);
    box-shadow: 0 0 0 0 rgba(0,224,255,0.7);
    animation: roi-pulse 2.4s ease-out infinite;
  }
  @keyframes roi-pulse {
    0% { box-shadow: 0 0 0 0 rgba(0,224,255,0.55); }
    70% { box-shadow: 0 0 0 12px rgba(0,224,255,0); }
    100% { box-shadow: 0 0 0 0 rgba(0,224,255,0); }
  }

  .roi-home-calc { perspective: 1800px; }
  .roi-stage {
    position: relative;
    border-radius: calc(var(--radius-xl) + 8px);
    padding: 7px;
    background: linear-gradient(155deg, rgba(0,224,255,0.6) 0%, rgba(0,180,255,0.18) 38%, rgba(17,53,92,0.5) 100%);
    box-shadow:
      0 50px 90px -30px rgba(0,0,0,0.7),
      0 0 0 1px rgba(0,224,255,0.22),
      0 0 70px -16px rgba(0,180,255,0.5);
    transform: rotateX(4deg);
    transform-origin: 50% 0;
    transition: transform 600ms var(--ease-out);
  }
  .roi-stage:hover, .roi-stage:focus-within { transform: rotateX(0deg); }
  .roi-stage::before {
    content: ''; position: absolute; inset: -40px; z-index: -1;
    background: radial-gradient(closest-side, rgba(0,180,255,0.32), transparent 70%);
  }
  .roi-stage__inner {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    background: #fff;
  }
  .roi-stage__inner::after {
    content: ''; position: absolute; inset: 0; pointer-events: none;
    background: linear-gradient(180deg, rgba(255,255,255,0.45), rgba(255,255,255,0) 16%);
  }
  .roi-stage :global(.roi-calc) { border: none; border-radius: var(--radius-xl); }
  .roi-stage :global(.roi-field label) { font-size: var(--fs-base); }
  .roi-stage :global(.roi-calc__ctas .btn) { min-height: 56px; padding: 1em 1.75em; font-size: var(--fs-base); }

  @media (prefers-reduced-motion: reduce) {
    .roi-showcase__pulse { animation: none; }
    .roi-stage { transform: none; }
  }

  /* ===== Dark brushed metal plate (vertical grain, light-blue tint) ===== */
  .roi-steel .roi-stage__inner {
    background-image:
      /* soft light-blue reflections (top + lower-left), like the reference */
      radial-gradient(150% 80% at 50% -10%, rgba(150,185,220,0.22), transparent 42%),
      radial-gradient(120% 70% at 18% 108%, rgba(120,165,210,0.16), transparent 48%),
      /* vertical brushed grain (north-south) */
      repeating-linear-gradient(90deg,
        rgba(210,230,255,0.05) 0px, rgba(210,230,255,0.05) 1px,
        rgba(0,0,0,0.22) 1px, rgba(0,0,0,0.22) 2px,
        rgba(210,230,255,0.025) 2px, rgba(210,230,255,0.025) 3px),
      /* dark blue-charcoal base, brighter in the center (vignette) */
      radial-gradient(130% 130% at 50% 38%, #2c3c50 0%, #1d2a39 46%, #121b27 100%);
    background-size: 100% 100%, 100% 100%, 3px 100%, 100% 100%;
    background-repeat: no-repeat, no-repeat, repeat, no-repeat;
    box-shadow:
      inset 0 1px 0 rgba(180,205,235,0.18),
      inset 0 -8px 20px rgba(0,0,0,0.5),
      inset 0 0 0 1px rgba(120,150,185,0.20);
  }
  .roi-steel .roi-stage__inner::after {
    background: linear-gradient(180deg, rgba(190,215,240,0.16), rgba(190,215,240,0) 16%);
  }
  .roi-steel :global(.roi-calc) { background: transparent; }

  /* Dark theme: lighten text so it reads on the metal */
  .roi-steel :global(.roi-calc__title),
  .roi-steel :global(.roi-field label) { color: #EAF1F8; }
  .roi-steel :global(.roi-calc__sub) { color: rgba(206,222,238,0.78); }
  .roi-steel :global(.roi-calc__header .eyebrow) { color: var(--color-glow); }
  .roi-steel :global(.roi-field output) { color: var(--color-glow); }
  .roi-steel :global(.roi-field__scale) { color: rgba(200,216,232,0.6); }
  .roi-steel :global(.roi-calc__disclaimer) { color: rgba(196,212,230,0.62); }

  /* Inputs as dark inset controls on the plate */
  .roi-steel :global(.roi-field select),
  .roi-steel :global(.roi-field__numeric input) {
    background: rgba(12,20,30,0.55);
    border-color: rgba(130,160,195,0.4);
    color: #EAF1F8;
    box-shadow: inset 0 1px 3px rgba(0,0,0,0.4);
  }
  .roi-steel :global(input[type="range"]) {
    background: rgba(8,14,22,0.55);
    box-shadow: inset 0 1px 2px rgba(0,0,0,0.5);
  }

  /* Results panel reads as an inset screen on the metal */
  .roi-steel :global(.roi-calc__results) {
    box-shadow: inset 0 0 0 1px rgba(140,170,205,0.28), 0 10px 26px rgba(0,0,0,0.45);
  }

  /* "See full methodology" outline button on dark metal */
  .roi-steel :global(.roi-calc__ctas .btn-outline) {
    background: rgba(255,255,255,0.08);
    color: #EAF1F8;
    border-color: rgba(180,205,235,0.4);
  }
  .roi-steel :global(.roi-calc__ctas .btn-outline:hover) {
    background: rgba(255,255,255,0.16);
    border-color: rgba(180,205,235,0.7);
    color: #fff;
  }

  /* ===== Wide, squat layout so the header + calculator fit one screen ===== */
  .section-roi { padding-top: var(--space-8); padding-bottom: var(--space-9); }
  .roi-showcase .roi-home-intro { margin-bottom: var(--space-5); }
  .roi-home-calc { max-width: 1140px; }
  .roi-stage :global(.roi-calc) { padding: clamp(1.25rem, 2.5vw, 1.9rem); }
  .roi-stage :global(.roi-calc__header) { margin-bottom: var(--space-3); }
  .roi-stage :global(.roi-calc__title) { font-size: var(--fs-lg); }
  /* The section intro above already explains how to use it, so drop the
     calculator's own sub-line on the homepage to save height. */
  .roi-stage :global(.roi-calc__sub) { display: none; }
  .roi-stage :global(.roi-calc__results) { margin-top: 0; padding: var(--space-5); }
  .roi-stage :global(.roi-calc__annual) { font-size: clamp(2.5rem, 1.6rem + 3.2vw, 4rem); margin: var(--space-2) 0; }
  .roi-stage :global(.roi-calc__secondary) { margin-top: var(--space-4); padding-top: var(--space-4); }
  .roi-stage :global(.roi-calc__context) { margin-top: var(--space-3); }
  .roi-stage :global(.roi-calc__ctas) { margin-top: var(--space-4); }
  .roi-stage :global(.roi-calc__disclaimer) { max-width: none; text-align: center; }

  @media (min-width: 820px) {
    .roi-stage :global(.roi-calc) {
      display: grid;
      grid-template-columns: 1.02fr 0.98fr;
      grid-template-areas:
        "header   header"
        "controls results"
        "ctas     results"
        "disc     disc";
      column-gap: clamp(1.5rem, 3vw, 2.5rem);
      row-gap: var(--space-4);
      align-items: start;
    }
    .roi-stage :global(.roi-calc__header) { grid-area: header; }
    .roi-stage :global(.roi-calc__controls) { grid-area: controls; align-content: start; gap: var(--space-4); }
    .roi-stage :global(.roi-calc__results) {
      grid-area: results;
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    .roi-stage :global(.roi-calc__ctas) {
      grid-area: ctas;
      justify-content: flex-start;
      align-self: start;
      margin-top: var(--space-2);
    }
    .roi-stage :global(.roi-calc__disclaimer) { grid-area: disc; }
  }
  .roi-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-8);
    align-items: center;
  }
  /* Mobile: visual first, copy second */
  .roi-grid__visual { order: -1; }
  @media (min-width: 880px) {
    .roi-grid { grid-template-columns: 6fr 4fr; gap: var(--space-9); }
    .roi-grid__visual { order: 0; }
  }
  .roi-grid__copy h2 { margin-top: var(--space-2); }
  .roi-grid__copy .lead { margin: var(--space-4) 0 var(--space-6); }
  .roi-grid__ctas {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    align-items: stretch;
  }
  @media (min-width: 480px) {
    .roi-grid__ctas { flex-direction: row; align-items: center; }
  }

  /* Stylized calculator illustration */
  .roi-illustration {
    background: linear-gradient(160deg, var(--color-ink-900) 0%, var(--color-ink-700) 100%);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
    color: #fff;
    box-shadow: 0 30px 60px rgba(10, 37, 64, 0.18);
    position: relative;
    overflow: hidden;
    aspect-ratio: 1 / 1;
    max-width: 460px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    isolation: isolate;
  }
  .roi-illustration::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -30%;
    width: 80%;
    height: 80%;
    background: radial-gradient(circle, rgba(0, 224, 255, 0.25) 0%, transparent 70%);
    filter: blur(40px);
    z-index: -1;
  }
  .roi-illustration__chip {
    display: inline-flex;
    flex-direction: column;
    gap: 4px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: var(--radius-md);
    padding: var(--space-3) var(--space-4);
    width: fit-content;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  .roi-illustration__chip-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-glow);
    font-weight: 600;
  }
  .roi-illustration__chip-value {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(2rem, 1.5rem + 2vw, 3rem);
    letter-spacing: -0.03em;
    color: #fff;
    line-height: 1;
  }
  .roi-illustration__bars {
    display: flex;
    align-items: flex-end;
    gap: 8px;
    height: 110px;
    margin: var(--space-5) 0;
  }
  .roi-illustration__bars div {
    flex: 1;
    height: var(--h);
    background: linear-gradient(180deg, var(--color-glow) 0%, var(--color-accent-500) 100%);
    border-radius: 6px 6px 0 0;
    opacity: 0.85;
  }
  .roi-illustration__slider {
    margin-top: auto;
  }
  .roi-illustration__track {
    position: relative;
    height: 8px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-pill);
    margin-bottom: var(--space-3);
  }
  .roi-illustration__fill {
    position: absolute;
    inset: 0 35% 0 0;
    background: linear-gradient(90deg, var(--color-accent-500) 0%, var(--color-glow) 100%);
    border-radius: var(--radius-pill);
  }
  .roi-illustration__thumb {
    position: absolute;
    top: 50%;
    left: 65%;
    transform: translate(-50%, -50%);
    width: 22px;
    height: 22px;
    background: #fff;
    border-radius: 50%;
    box-shadow: 0 0 0 4px rgba(0, 224, 255, 0.35), 0 4px 12px rgba(0, 0, 0, 0.25);
  }
  .roi-illustration__caption {
    font-size: var(--fs-xs);
    color: rgba(255, 255, 255, 0.7);
    text-align: center;
    font-family: var(--font-mono);
    letter-spacing: 0.04em;
  }

  /* Leaders 2x2 grid */
  .leaders-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    margin-top: var(--space-7);
  }
  @media (min-width: 720px) {
    .leaders-grid { grid-template-columns: 1fr 1fr; gap: var(--space-7); }
  }
  .leader-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-7);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    transition: transform 320ms var(--ease-out), border-color 320ms var(--ease-out), box-shadow 320ms var(--ease-out);
  }
  .leader-card:hover {
    transform: translateY(-3px) scale(var(--hover-scale-card));
    border-color: var(--color-accent-500);
    box-shadow: 0 20px 40px rgba(10, 37, 64, 0.10);
    z-index: 3;
  }
  .leader-card__photo,
  .leader-card__placeholder {
    width: 96px;
    height: 96px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid var(--color-border);
    margin-bottom: var(--space-3);
  }
  .leader-card__placeholder {
    display: inline-grid;
    place-items: center;
    background: linear-gradient(135deg, var(--color-ink-800) 0%, var(--color-accent-500) 130%);
    color: #fff;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.75rem;
    letter-spacing: 0.06em;
    border-color: transparent;
  }
  .leader-card h3 {
    font-size: var(--fs-lg);
    display: inline-flex;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-bottom: 4px;
  }
  .leader-card__badge {
    display: inline-flex;
    align-items: center;
    gap: 0.35em;
    font-family: var(--font-sans);
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 0.3em 0.7em;
    background: rgba(0, 180, 255, 0.12);
    color: var(--color-ink-800);
    border: 1px solid rgba(0, 180, 255, 0.3);
    border-radius: var(--radius-pill);
    white-space: nowrap;
  }
  .leader-card__role {
    color: var(--color-accent-500);
    font-weight: 600;
    font-size: var(--fs-sm);
    margin-bottom: var(--space-3);
  }
  .leader-card p { color: var(--color-text-soft); font-size: var(--fs-sm); }
  .leader-card__social {
    list-style: none;
    padding: 0;
    margin: auto 0 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    padding-top: var(--space-5);
    border-top: 1px solid var(--color-border);
  }
  .leader-card__social a {
    display: inline-flex;
    align-items: center;
    gap: 0.5em;
    padding: 0.4em 0.85em;
    background: var(--color-bg-soft);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-pill);
    color: var(--color-ink-700);
    font-size: var(--fs-xs);
    font-weight: 500;
    transition: background var(--dur-fast) var(--ease-out), border-color var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out);
  }
  .leader-card__social a:hover {
    background: rgba(0, 180, 255, 0.08);
    border-color: var(--color-accent-500);
    color: var(--color-ink-800);
  }
  .leader-card__social svg { color: var(--color-ink-700); }
  .leader-card__social a:hover svg { color: var(--color-accent-500); }

  /* Industry cards */
  .industry-card {
    color: var(--color-text);
    position: relative;
    padding-bottom: var(--space-7);
  }
  .industry-card:hover { color: var(--color-text); }
  .industry-card h3 { margin-bottom: var(--space-3); }
  .industry-card .arrow {
    position: absolute;
    bottom: var(--space-5);
    right: var(--space-5);
    color: var(--color-accent-500);
    font-size: 1.25rem;
    transition: transform var(--dur-fast) var(--ease-out);
  }
  .industry-card:hover .arrow { transform: translateX(4px); }

  /* Clickable pillar cards (button reset + reveal-on-hover affordance) */
  .pillar-card {
    text-align: left;
    color: var(--color-text);
    font: inherit;
    cursor: pointer;
    width: 100%;
    background: var(--color-bg);
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }
  .pillar-card:focus-visible {
    outline: 2px solid var(--color-accent-500);
    outline-offset: 3px;
  }
  .pillar-card__more {
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    color: var(--color-accent-500);
    font-weight: 600;
    font-size: var(--fs-sm);
    margin-top: var(--space-4);
  }
  .pillar-card__arrow {
    display: inline-block;
    transition: transform var(--dur-fast) var(--ease-out);
  }
  .pillar-card:hover .pillar-card__arrow {
    transform: translateX(4px);
  }

  /* Pillar detail modal */
  .pillar-modal {
    position: fixed;
    inset: 0;
    z-index: 250;
    display: grid;
    place-items: center;
    padding: var(--space-4);
  }
  .pillar-modal[hidden] { display: none; }
  .pillar-modal__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(5, 11, 20, 0.72);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    animation: pillar-fade 220ms var(--ease-out);
  }
  .pillar-modal__panel {
    position: relative;
    width: 100%;
    max-width: 760px;
    max-height: calc(100vh - 2rem);
    overflow-y: auto;
    background: var(--color-bg);
    border-radius: var(--radius-xl);
    box-shadow: 0 32px 80px rgba(5, 11, 20, 0.45);
    animation: pillar-in 320ms var(--ease-out);
  }
  .pillar-modal__head {
    position: sticky;
    top: 0;
    background: var(--color-bg);
    z-index: 1;
    padding: var(--space-4) var(--space-5) 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .pillar-modal__head .card-num {
    font-family: var(--font-mono);
    font-size: var(--fs-xs);
    color: var(--color-accent-500);
    letter-spacing: 0.12em;
    font-weight: 600;
    margin-bottom: 0;
  }
  .pillar-modal__close {
    width: 44px;
    height: 44px;
    border-radius: var(--radius-md);
    color: var(--color-ink-800);
    display: inline-grid;
    place-items: center;
    transition: background var(--dur-fast) var(--ease-out);
  }
  .pillar-modal__close:hover { background: var(--color-bg-muted); }
  .pillar-modal__body {
    padding: var(--space-4) var(--space-7) var(--space-7);
  }
  .pillar-modal__icon {
    color: var(--color-accent-500);
    margin-bottom: var(--space-4);
  }
  .pillar-modal__body h2 {
    font-size: var(--fs-2xl);
    margin-bottom: var(--space-4);
  }
  .pillar-modal__body p {
    color: var(--color-text-soft);
    margin-bottom: var(--space-4);
  }
  .pillar-modal__outcomes-head {
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--color-ink-800);
    margin: var(--space-6) 0 var(--space-3);
    font-weight: 600;
  }
  .pillar-modal__outcomes {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-6);
    display: grid;
    gap: var(--space-3);
  }
  .pillar-modal__outcomes li {
    padding: var(--space-3) var(--space-4) var(--space-3) calc(var(--space-4) + 1.5em);
    background: var(--color-bg-soft);
    border-radius: var(--radius-md);
    border-left: 3px solid var(--color-accent-500);
    position: relative;
    color: var(--color-text);
  }
  .pillar-modal__outcomes li::before {
    content: '';
    position: absolute;
    left: calc(var(--space-4) + 4px);
    top: 1em;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--color-accent-500);
  }
  .pillar-modal__cta {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: var(--space-5);
  }
  @keyframes pillar-fade { from { opacity: 0 } to { opacity: 1 } }
  @keyframes pillar-in {
    from { opacity: 0; transform: translateY(16px) scale(0.98); }
    to { opacity: 1; transform: none; }
  }
  @media (prefers-reduced-motion: reduce) {
    .pillar-modal__backdrop, .pillar-modal__panel { animation: none; }
  }

  /* Three Things section */
  .section-three {
    background: linear-gradient(180deg, var(--color-bg) 0%, var(--color-bg-soft) 100%);
  }
  .three-grid {
    display: grid;
    gap: var(--space-5);
    grid-template-columns: 1fr;
    margin-top: var(--space-7);
  }
  @media (min-width: 880px) {
    .three-grid { grid-template-columns: repeat(3, 1fr); align-items: stretch; }
  }
  .three-card {
    position: relative;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-7);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    transition: transform 360ms var(--ease-out), border-color 360ms var(--ease-out), box-shadow 360ms var(--ease-out);
  }
  .three-card:hover {
    transform: translateY(-4px) scale(var(--hover-scale-card));
    border-color: var(--color-accent-500);
    box-shadow: 0 24px 48px rgba(10, 37, 64, 0.10);
    z-index: 3;
  }
  .three-card--featured {
    background: linear-gradient(180deg, var(--color-ink-900) 0%, var(--color-ink-700) 100%);
    color: var(--color-text-on-dark);
    border-color: transparent;
    box-shadow: 0 20px 40px rgba(10, 37, 64, 0.18);
  }
  .three-card--featured h3 { color: #fff; }
  .three-card--featured p { color: var(--color-text-on-dark-soft); }
  .three-card--featured:hover { border-color: var(--color-accent-500); }
  .three-card__icon {
    width: 56px;
    height: 56px;
    border-radius: var(--radius-md);
    display: inline-grid;
    place-items: center;
    background: linear-gradient(135deg, rgba(0, 180, 255, 0.12) 0%, rgba(0, 180, 255, 0.04) 100%);
    color: var(--color-accent-500);
    border: 1px solid rgba(0, 180, 255, 0.2);
  }
  .three-card--featured .three-card__icon {
    background: rgba(0, 224, 255, 0.16);
    color: var(--color-glow);
    border-color: rgba(0, 224, 255, 0.3);
  }
  .three-card h3 {
    font-size: var(--fs-lg);
    line-height: 1.2;
  }
  /* three-card as a clickable popup trigger */
  button.three-card {
    width: 100%;
    text-align: left;
    font: inherit;
    cursor: pointer;
    -webkit-appearance: none;
    appearance: none;
  }
  .three-card__more {
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    margin-top: auto;
    font-weight: 600;
    font-size: var(--fs-sm);
    color: var(--color-accent-500);
    opacity: 0;
    transform: translateY(4px);
    transition: opacity var(--dur-base) var(--ease-out), transform var(--dur-base) var(--ease-out);
  }
  .three-card--featured .three-card__more { color: var(--color-glow); }
  .three-card:hover .three-card__more,
  .three-card:focus-visible .three-card__more { opacity: 1; transform: translateY(0); }
  .three-card__more-icon { font-size: 1.2em; line-height: 1; }
  @media (prefers-reduced-motion: reduce) {
    .three-card__more { opacity: 1; transform: none; }
  }

  /* why-us cards as links */
  .why-link {
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }
  .why-link .why-more {
    margin-top: auto;
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    font-weight: 600;
    font-size: var(--fs-sm);
    color: var(--color-glow);
    opacity: 0;
    transform: translateY(4px);
    transition: opacity var(--dur-base) var(--ease-out), transform var(--dur-base) var(--ease-out);
  }
  .why-link:hover .why-more,
  .why-link:focus-visible .why-more { opacity: 1; transform: translateY(0); }
  @media (prefers-reduced-motion: reduce) {
    .why-link .why-more { opacity: 1; transform: none; }
  }

  /* Podcast section with YouTube embed */
  .dd-section {
    background-image:
      radial-gradient(circle at 80% 10%, rgba(0, 224, 255, 0.12) 0%, transparent 50%),
      radial-gradient(circle at 10% 90%, rgba(0, 180, 255, 0.10) 0%, transparent 55%),
      var(--color-ink-900);
  }
  .dd-head {
    text-align: center;
    max-width: 60ch;
    margin: 0 auto var(--space-7);
  }
  .dd-head h2 {
    color: #fff;
    margin-top: var(--space-2);
  }
  .dd-head .lead {
    color: var(--color-text-on-dark-soft);
    margin: var(--space-4) auto var(--space-5);
  }
  .dd-logotype {
    display: inline-flex;
    align-items: center;
    gap: 0.6em;
    padding: 0.45em 1.1em;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-pill);
    color: var(--color-glow);
    font-family: var(--font-display);
    font-weight: 600;
    font-size: var(--fs-md);
    letter-spacing: -0.01em;
  }
  .dd-logotype__words { color: #fff; }
  .dd-video-wrap {
    display: flex;
    justify-content: center;
    margin: 0 auto var(--space-7);
  }
  .dd-video {
    position: relative;
    width: 100%;
    max-width: 960px;
    padding-bottom: 56.25%;
    height: 0;
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.5);
    background: #000;
  }
  .dd-video iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
  }
  .dd-ctas {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    justify-content: center;
    align-items: stretch;
    max-width: 760px;
    margin: 0 auto;
  }
  @media (min-width: 640px) {
    .dd-ctas { flex-direction: row; align-items: center; }
  }
  .btn-ghost.on-dark {
    color: var(--color-glow);
  }
  .btn-ghost.on-dark:hover {
    color: #fff;
  }
</style>
